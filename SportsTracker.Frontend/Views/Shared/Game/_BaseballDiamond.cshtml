@model SportsTracker.Shared.Models.Sport.BaseballSituation

<div class="diamond">
    <div class="base second @(Model.OnSecond ? "occupied" : "")"></div>

    <div class="middle">
        <div class="base third @(Model.OnThird ? "occupied" : "")"></div>

        <div class="base home"></div>

        <div class="base first @(Model.OnFirst ? "occupied" : "")"></div>
    </div>

</div>