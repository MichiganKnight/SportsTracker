@model SportsTracker.Frontend.ViewModels.LeagueSectionViewModel

<div class="league-header d-flex justify-content-between align-items-center">
    <h3 class="mb-0">
        @Model.Icon
        
        @Model.LeagueName
    </h3>
    
    <a href="@Model.Route" class="text-decoration-none">
        View All →
    </a>
</div>